Currently there is no documentation on pt_extbase. We hope to bring up some documentation during the next couple of months.

The installation is quite straight forward by using Extension Manager. There are no further dependencies except Extbase.

= Changelog =
V 0.0.3
- FIX: Fixed a bug in namespace utility, which was responsible for unused array keys in the session array. (Broke realurl config)